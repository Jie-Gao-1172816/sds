1. #整体结构
2. 颜色
3. student list reshrech allow emptye add flash messuage on base   if q and len(students) == 0:
    flash("No students matched your search.", "warning")
4. mouse hover change color mouse cliek change color mouse 

5. <!-- 
        Only display class details when the class_id changes.
        This groups students under the same class.-->
      {% if class_row['class_id'] != ns.prev_class_id %}
6. let each class have same color 
6. class list 和 summary 合并哪些 数据库
7. SIMMARY 显示哪些信息
8. back to list 浅色 不重要 
9.  enrol     if not sid or not class_id:
            flash('Please select a student and a class.')